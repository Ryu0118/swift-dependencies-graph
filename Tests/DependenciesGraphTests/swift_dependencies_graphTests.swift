import XCTest
@testable import swift_dependencies_graph

final class swift_dependencies_graphTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(swift_dependencies_graph().text, "Hello, World!")
    }
}
